# K6: design review verdict (attempt 2)

**PASS.**

For this change only, the orchestrator allowed me to read two builder files: `review/builder-notes/k6.md` and `k6-kaynaklar.md`. I read no code or diffs. On 24 Sep I checked the claims against the signed-in summary of Actions run 35930076552, against the public GitHub API, and with my own live requests from this Mac. Screenshots are in `review/shots/k6/reviewer/deneme2/`.

- **K6-1 (diagnosis): PASS.**
  - **Elbit.** CI probe 1 requested today's entry URL, `https://elbitsystems.com/feed/`, with the collector's headers. The redirect chain was 301 → `www.elbitsystems.com/feed/` → 301 → `/news`. The final response was 200 `text/html`, 68,151 B, with 0 `<item>`/`<entry>` elements, and the first 500 bytes are a Drupal HTML page.
    - Cause: the feed has been removed and its URL now redirects to the news page.
    - This explains "feed parsed but empty". It was run from the runner, so it is the same vantage as the recorded failure.
  - **Northrop.** Probe 4: 403 Akamai "Access Denied", 413 B. Probe 5: 200 `application/rss+xml`, 10 items. My own requests gave the same results: 403 without Accept-Language, 200 with it.
  - Evidence: the tani section of run 35930076552 and `after/A-k6-tani-girisli.jpg`.
- **K6-2 (working source per company): PASS.**
  - **Elbit:** the source is the `www.elbitsystems.com/news` page, read as HTML with the `elbitsystems-news` parser. It gives 10 items. The last 3 are dated 18, 14 and 9 Sep 2026, and the hand-off note gives absolute URLs for them. The offline test is green.
  - **Northrop:** the RSS feed plus the header gives 10 items. I saw the last 3 live: 17, 14 and 10 Sep 2026, with the same titles.
  - **Elbit Systems UK:** an extra source, with 20 items.
  - **Limit:** Elbit's `/news` returns 403 from my network, in both curl and Chrome, so for Elbit the runner result is the only evidence.
  - Evidence: `deneme2/A-run-35930076552-ayristirici.jpg`.
- **K6-3 (A): PASS.**
  - The run's `ayristirici` job is green (API conclusion `success`). Its summary shows "🟢 27/27 kontrol · ağ kütüphanesi 0".
  - The table has rows for Elbit Systems (html · elbitsystems-news, 10), Northrop (rss + Accept-Language, 10) and Elbit Systems UK (20).
  - Evidence: `deneme2/A-run-35930076552-ayristirici.jpg`.
  - The supplied `after/A-k6-tani-girisli.jpg` is cropped above this table, so I captured it myself from the signed-in run page.
- **K6-4 (hand-off paste-ready): PASS.**
  - **Northrop:** a find/replace that adds the field in front of `url`. This fixes the attempt-1 defect, where the block was a whole object.
  - **Elbit:** two find/replaces, limited to its own entry. The URL it searches for is the one CI requested.
  - **Elbit Systems UK:** optional, one line with its trailing comma after the opening `[`.
  - **Timing:** "Ne zaman" now says to paste on merge day, and explains what happens if pasted early. The claim that the code "güncellendi" is gone.
  - Headlines, dates and counts match the (A) summary.

**Regressions: none.**
- On http://localhost:8000/reports/2026-09-23.html the Rev 28 line "Elbit Systems ve Northrop Grumman'ın kendi duyuruları bugün okunamadı." appears once.
- At 1440 it looks the same as attempt 1's shots, in light and dark: `deneme2/reports-2026-09-23-1440-{light,dark}.png`.
- At 375 light it sits under TARAMA and there is no horizontal overflow (scrollWidth 371 = innerWidth): `deneme2/reports-2026-09-23-375-light-caveat.jpg`.
- I could not produce a new 375 dark capture because headless Chrome failed at 375 px. Attempt 1's `reviewer/reports-2026-09-23-375-dark-caveat.png` stands for that view, and K6 changes no page.

**Concern:** the Elbit parser reads only page 1 of `/news` and is untestable from a Turkish IP. It depends on markup that a redesign would silently turn into "feed parsed but empty", at which point the Rev 28 line returns.
