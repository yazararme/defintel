# K6 — repair the Elbit and Northrop feeds · builder notes

Branch `rev21-33`, not committed. Customer paste note: `review/builder-notes/k6-kaynaklar.md`.

## K6-1 · Diagnosis (live, 24 Sep 2026 ~01:00–01:10 TSİ, from the Mac, a Turkish connection)

Requests copied `read_feed()` exactly: `requests.get`, `User-Agent` = collect_news `UA`
(Chrome/140 on macOS), `Accept: */*`, timeout 30. collect_news uses no proxy for feeds
(`probe_proxy.py` only probes the Google Translate reading proxy), so there was nothing further to copy.

**Northrop Grumman** — `https://investor.northropgrumman.com/rss/news-releases.xml` (Nasdaq gcs-web
behind Akamai).

| Request | Result |
|---|---|
| collect_news headers (Chrome UA, `Accept: */*`) | **403**, 415 B Akamai "Access Denied … Reference #18.3fb3afc3…" (same as 17–23 Sep) |
| Chrome UA only / Chrome UA + `Accept: application/rss+xml…` | 403 |
| Chrome UA + `Accept: */*` + **`Accept-Language: en-US,en;q=0.9`** | **200**, `application/rss+xml`, 7,885 B, 10 items (same with curl, so not a client-library artefact) |
| `python-requests/2.32` UA, no other header | 200 |
| custom bot UA | read timeout (tarpit) |

Cause: bot fingerprint — a request that says it is Chrome but sends no Accept-Language. The URL
is right and the feed is alive. Fix route **(b)**, a per-source request header.

**Elbit Systems** — the source's `url` is not in the repo (only in Drive `kaynaklar.json`; not in
`data/news`, the built pages or git history). CI records "feed parsed but empty" = HTTP 2xx whose
body has no feed entries. From here every Elbit parent address refuses:

| URL | Result |
|---|---|
| `https://elbitsystems.com/` , `/feed/`, `/robots.txt`, `/media-resources/press-releases/`, `www.` | **403** `awselb/2.0` (AWS ALB/WAF), 520 B, with any UA, with full browser headers, with curl — i.e. an IP/geo block, not a header one |
| `https://ir.elbitsystems.com/rss/news-releases.xml` (gcs-web + Akamai, same platform as Northrop) | without Accept-Language: 403 Akamai; with it: **401** "Authentication failed. Please try again." (40 B); `/rss/events.xml` the same |
| `https://ir.elbitsystems.com/`, `/news-releases` | TCP accepted, no response (HTTP/2 stream reset or timeout) |

The same gcs-web platform serves Northrop's RSS to the same IP, so the IR 401 is Elbit's own
setting. It may be a geo block for Turkey or a closed feed. I cannot see what the US-based runner
receives, so the parent feed is **not repaired**. The customer is asked for the entry's `url`.
Rev 28's line stays for Elbit, as the brief says.

Working Elbit-owned replacement, route **(c)**:
`https://www.elbitsystems-uk.com/media-events/recent-news` (Cloudflare, 200, 47 KB static HTML,
20 `<li class="media">` rows each with `<h3>DD Month YYYY</h3>` and a relative link). It carries the
UK subsidiary only, about 4 items in 2026 so far. `player_announcement()` maps its headlines to `elbit`
("Elbit Systems UK Showcases …" → Rakip Duyuruları / elbit).

## Code

- `scripts/collect_news.py`
  - `request_headers(source)`: `{UA, Accept: */*}` + the source's optional `istek_basligi` dict.
    Sources without the field send exactly the old headers (tested).
  - `parse_body(source, content)`: the old feedparser block moved out of `read_feed()` unchanged;
    `tur: "html"` dispatches to `HTML_PARSERS[source["ayristirici"]]` (stdlib; an unknown name raises
    and becomes the source's error).
  - `parse_elbitsystems_uk()`: a narrow regex parser for that one page. After a site redesign it
    returns 0 items, so the source reads "feed parsed but empty" like a dead feed. It never invents
    rows.
  - The source filter accepts `tur in ("rss", "html")` (was `== "rss"`).
  - Fixture path: a feed may carry `{"body": "…"}`, which goes through `parse_body()`.
- `scripts/test_k6.py` (new, stdlib + feedparser when present) — 16 checks. A fake `requests`
  mimics Akamai (Chrome UA without Accept-Language → 403): the old Northrop entry reproduces the
  exact recorded error and the new entry gets through. The Northrop RSS fixture gives 10 items and the
  expected last 3. The Elbit UK parser gives 20 dated absolute-URL items and the expected last 3. A
  403 page or empty body gives 0 items. An unknown parser raises. An offline `collect_news.py
  --fixture` run (network-library trap from test_collect) reads both sources, with 0 failures and
  SİLME-YOK 2/2 equal. `data/news` is untouched.
  feedparser is not installed on the Mac, so the feedparser parse of the Northrop RSS is skipped
  there ("⚪ atlandı") and checked by stdlib instead. The workflow installs feedparser and runs it.
- `scripts/fixtures/k6-northrop-news-releases.xml`, `k6-elbitsystems-uk-recent-news.html.txt` (.txt so build.py's link check and Pages do not treat it as a page): raw
  live responses, unedited.
- `.github/workflows/k6-kaynak-test.yml` (**K6-3 (A)**): push to `rev21-33` only (+ dispatch),
  `pip install feedparser`, `python3 scripts/test_k6.py`. No secret and no real collection. The
  summary shows a table of both sources (type, request fix, parsed count, last 3 headlines) and
  a 🟢 line. Delete it before merge.
- `review/tools/smoke.py`: `k6_checks()` runs test_k6.py, checks the workflow (rev21-33 only, no
  secret, no real collection) and parses the JSON blocks in k6-kaynaklar.md.

## Not done / for the customer

- Drive is not touched. Both edits are the customer's to make (k6-kaynaklar.md).
- The Northrop fix is proven from a Turkish IP. GitHub's Azure IPs may score differently with
  Akamai. The first morning run after the paste shows it: if `failures[]` no longer has Northrop,
  Rev 28's line drops Northrop by itself.
- Elbit parent: needs the current `url`, and ideally one fetch from a non-Turkish vantage.

## Attempt 2 (24 Sep 2026, ~01:20–01:30 TSİ) — verdict k6-deneme1 FAILs

**K6-1 Elbit.** The entry's URL is now known (orchestrator note, read from Drive):
`https://elbitsystems.com/feed/`. Requested from here with collect_news headers, with and without
Accept-Language: **403** `text/html`, 520 B nginx-style "403 Forbidden" page, Server `awselb/2.0`, no
redirects. Same as every elbitsystems.com path in attempt 1, so this vantage cannot see what CI sees
(CI: 2xx, "feed parsed but empty"). The diagnosis is therefore made from CI:

- `scripts/k6_tani.py` (new) — one plain GET per probe with `collect_news.request_headers()` and
  `TIMEOUT`, redirects followed as `requests` does. Probes: (1) Elbit `/feed/` exactly as today,
  (2) same + Accept-Language, (3) Northrop RSS without the field, (4) Northrop RSS with
  `istek_basligi` (proves the repair from the runner IP), (5) Elbit IR RSS + Accept-Language,
  (6) elbitsystems.com press-releases page (candidate for route c), (7) Elbit UK page. Only Elbit /
  Northrop hosts (test_k6 checks it). No secret, no writes, always exit 0.
- (A) summary: two bold top lines — "Elbit — CI'daki arızanın nedeni (1)" and "Northrop — K6
  onarımı bu IP'den (4)" — then a table (probe, status, Content-Type, parsed count, one-line
  interpretation), then per probe: request headers, status · Content-Type · Server · size · final
  URL, redirect chain with Location, raw `<item>/<entry>` count, first 500 bytes escaped (control
  chars as \xNN, HTML-escaped in `<pre>`), and the interpretation. Full bodies (≤300 KB) go to the
  job log so a route-(c) parser can be written from them.
- `yorumla()` names: 401/403/429/5xx with who (Akamai / awselb / Cloudflare …); 2xx empty body;
  2xx bot-challenge page (Cloudflare / Imperva / AWS WAF / CAPTCHA); 2xx valid feed shell with no
  items (feed emptied); 2xx HTML with meta/JS redirect; 2xx plain HTML page (feed URL now serves a
  page); ÇALIŞIYOR with count and newest item.
- `k6-kaynak-test.yml`: new job `tani`, `if: github.event_name == 'push' && github.ref ==
  'refs/heads/rev21-33'`, `pip install requests feedparser`, `timeout-minutes: 10`; the offline
  `ayristirici` job is unchanged; `scripts/k6_tani.py` added to the push paths. Local dry run of
  k6_tani.py from the Mac: 1/2 403 awselb, 3 403 Akamai, 4 200 RSS 10 items, 5 401, 6 403 awselb,
  7 200 Elbit UK 20 items.

Elbit repair (a/b/c) is re-decided from that (A) output; nothing for the Elbit parent is proposed to
the customer until then. Elbit Systems UK stays as the extra source.

**K6-4.** `k6-kaynaklar.md` rewritten: Northrop is a find/replace of the `"url":"…xml"` text with
`"istek_basligi":{…},"url":"…xml"` (field inserted before url, so trailing commas and other fields are
untouched, compact or spaced); Elbit UK is one line with a trailing comma, pasted right after the
opening `[`. "Ne zaman": after the branch is merged to main (merge day); pasted earlier it is
harmless but ineffective. No more "kod güncellendi" and no request for the URL.

Live re-check 01:25 TSİ: Northrop with field 200, 10 items, same last 3; without 403. Elbit UK: one
HTTP 500 once, then 200, 20 items, same last 3 (collect_news retries twice).

**Tests.** test_k6.py 21/21 (5 new: interpretation of HTML page / empty shell / Cloudflare / awselb
403 / empty body, redirect chain in the line, Elbit UK fixture → ÇALIŞIYOR 20, summary shows status,
type, chain, escaped first 500 bytes, host allow-list). smoke.py `k6_checks`: tani job only on
rev21-33 push and offline job present; the note's replace text and UK line applied to sample
kaynaklar.json lists (compact, spaced, indented) give valid JSON with nothing else changed.

## Attempt 2, continued — CI diagnosis result and Elbit repair (24 Sep 2026, ~01:40 TSİ)

**Diagnosis (run 35928738920, both jobs green, runner IP; log read from the saved job log).**
- Elbit `https://elbitsystems.com/feed/` → `301` → `https://www.elbitsystems.com/feed/` → `301`
  (Location `/news`) → **200** `text/html; charset=utf-8`, Server nginx, 68,151 B: the Drupal "News |
  Elbit Systems" page. Same with Accept-Language. **Cause of "feed parsed but empty": the feed has
  been removed and its URL redirects to the HTML news page.** feedparser finds no entries there.
  (From TR the same host answers 403 awselb, so this was only visible from CI.)
- Northrop without the field: 403 Akamai (415 B). With `istek_basligi` Accept-Language: **200**
  `application/rss+xml`, 7,885 B. **The repair works from the runner IP too.**
- Elbit IR RSS: 401 (40 B). Old press-release path: 404 after a redirect to www (dropped as a probe).
  Elbit UK: 200 cloudflare, 47,039 B.

**Repair, route (c).** `collect_news.parse_elbitsystems_news()` registered as `HTML_PARSERS
["elbitsystems-news"]`: splits the page into `new-first-item views-row` / `new-box views-row` blocks.
For each block it reads the first link href (made absolute), `<time datetime="…Z">` and the title from
`<h2 class="title">` (lead item) or `<div class="title"><span>` (the rest). Page 1 only (10 releases,
about 2 months). A redesign gives 0 items, the same as a dead feed ("feed parsed but empty").
Fixture: `scripts/fixtures/k6-elbitsystems-news.html.txt` is the CI-captured body, cut from the
listing's `view-content` up to the pager (13 KB), with a provenance comment. The markup is unedited.
Result on the fixture: **10 items**, all dated with absolute www.elbitsystems.com/news/ URLs. The last 3:
18 Sep "Elbit Systems Files a Shelf Prospectus in Israel"; 14 Sep "Successful Live Demonstration of
the Loitering System: Diehl Defence and Elbit Systems successfully demonstrated SkyStriker’s
Capabilities"; 9 Sep "One2Many: Elbit Systems’ FUSE Introduces Military-Grade Autonomous Systems
Built for Scale".

- `test_k6.py` 27/27. New checks:
  - /news gives 10 dated absolute items and the expected last 3;
  - `read_feed` returns the same through the fake network;
  - a 403 page, an empty body or the UK page each give 0 items;
  - the offline collection now reads 3 sources (Elbit 3, UK 2, Northrop 3 in window), with 0 failures and SİLME-YOK 3/3;
  - k6_tani on the /news fixture prints ÇALIŞIYOR 10, and its summary top line shows the count and the last 3;
  - the (A) table has an Elbit Systems row.
- `k6_tani.py`: new probe 2 "Elbit Systems · /news · html `elbitsystems-news`". The summary opens with the
  bold line "Elbit — K6 onarımı /news (2): … Son 3: …", and the Northrop line also lists its last 3.
- `k6-kaynaklar.md` changes:
  - Elbit Systems gets two find/replaces inside its own entry only: `"url":"https://elbitsystems.com/feed/"` →
    `"url":"https://www.elbitsystems.com/news","ayristirici":"elbitsystems-news"` and `"tur":"rss"` →
    `"tur":"html"`.
  - The "no change / waiting for diagnosis" section is gone.
  - Elbit Systems UK is **kept, as an optional extra (recommended)**. It is not redundant: its 16 Sep and 7 Sep
    releases are not on the parent /news page 1, and the parent page has only one UK item (17 Jul
    Farnborough, absent from the UK page's 20). The collector's same-headline dedupe covers any overlap.
- smoke `k6_checks`: the note's 7 text blocks are applied to sample lists in compact, spaced and indented
  form. The result must be valid JSON; the Elbit entry must become exactly url /news + ayristirici +
  tur html, with `site` kept; Northrop's `tur` must stay rss.
