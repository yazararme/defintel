# K2 — builder notes (iki oyuncu listesi, iki etiket)

Branch `rev21-33`. Nothing committed or pushed, no workflow triggered, no secrets, no `gh` write,
`worker/` untouched. The tree is left built in the normal state (alias testi 64/64).

## What changed

| File | Change |
|---|---|
| `build.py` (report rail) | (see attempt 2 for what the rail now counts) The Oyuncular rail block is now `OYUNCULAR` label → `<span class="rail-sub rail-src">brifingde geçen</span>` → names. It reuses `.rail-sub`, the rail's existing mono, 11px, `--muted` sub-line voice. At 1440 it is its own line under the label. At ≤920 the block is a wrapped row: `OYUNCULAR brifingde geçen Rheinmetall · …`. |
| `build.py` `players_page` | 64/64 state: `Bugün <strong data-tally="today">15</strong> <span class="tally-src">başlıklarda geçen</span> · son 30 günde 27 · izlenen 64`. The label sits right after the day's number, the number it describes. Below 64/64 nothing changes: the line is only `izlenen 64`, so there is no label (Rev 21 all-or-nothing). |
| `assets/app.css` | `.tally-src`: mono, 12px, `--muted`, `nowrap`. There is no new colour or token. |
| `review/tools/smoke.py` | Adds `K2_JS` and `k2_check()`, which run in both states. In the 64/64 state (after `--oyuncular`): at 375×812 and 1440×900, light and dark, the latest report's rail has "brifingde geçen" visible, before the names, in the same font family and colour as `.rail-label`. `#ptally` matches `Bugün N başlıklarda geçen · son 30 günde N · izlenen N`, the label is the next sibling of the `today` cell, it is mono and `--muted`, and the line stays correct and changes with the Mühimmat chip on. There is no horizontal overflow. In the KAPSAM_BOZ=thales state the line is `izlenen N` (also with Mühimmat) and "başlıklarda geçen" appears nowhere on the page. |

`app.js` did not need to change. The chip recount rewrites only the `[data-tally]` cells, so the label stays in place: with Mühimmat on, the line reads `Bugün 6 başlıklarda geçen · son 30 günde 9 · izlenen 23`.
`check_reports.py --oyuncular` is unchanged and still green, because the line still starts with "Bugün " and ends with "izlenen N".

## İLK-EKRAN

At 375 the rail sits behind the summary, so the label cannot move the executive's first screen.
- Local: `h2#ozet` is at 280px and item 4 ends at 773px. The rail top is at 924px.
- Worst case (smoke, the "ilk:" token on its own line, plus 0.15px letter-spacing): h2 280px, item 4 ends at 799px, which is ≤812.

## Attempt 2 — each list now counts what its label says (orchestrator decision)

The first pass flagged two premises in the brief that did not hold. The orchestrator ruled that both lists must count exactly what their labels say.

| Where | Change | 23 Sep result |
|---|---|---|
| `build.py` report rail | Only briefing-body matches, each linked to its `#g` anchor: rivals in body order, then Turkish players in config order. Rev 17's headline-scan additions are no longer in the rail; they stay findable through medya takibi `?oyuncu=` and /oyuncular.html. | Rheinmetall · BAE Systems · CSG · Leonardo · Kongsberg · Hensoldt · Aselsan (7; Roketsan and Baykar dropped) |
| `build.py` `player_history` | `son` and `gun_30g` now come only from headline matches. `gunler`, the archive `?oyuncu=` day list, stays the union and is byte-identical. | — |
| `build.py` `players_page` | Takes the latest source day as "bugün" instead of the largest `son`. Otherwise, on a day with no headline match, yesterday would be printed as "bugün". | "Bugün 14 başlıklarda geçen · son 30 günde 24 · izlenen 64" (was 15 / 27); Mühimmat 5 / 7 / 23 |

Consequences of these changes:
- **Rails on earlier days** also lost their headline-only names:
  - 17 Sep: Aselsan, Havelsan, FNSS
  - 18 Sep: Aselsan, Havelsan, FNSS
  - 19 Sep: Aselsan, Otokar
  - 20 Sep: Aselsan
  - 22 Sep: Baykar
  - 23 Sep: Roketsan, Baykar
  - 14, 15, 16 and 21 Sep are unchanged. `index.html` follows 23 Sep.
- **/oyuncular.html rows:**
  - CSG, NORINCO and Olin had only briefing mentions in the window, so they are now faded with no token.
  - "N gün" is lower for Rheinmetall (9→7), Hanwha (8→5), Aselsan (7→6), MBDA (4→3), Kongsberg (3→2) and Rafael (3→2).
  - The sort changed, so the first rows are now Rheinmetall, PGZ, Leonardo, KNDS, BAE Systems.
- **`data/oyuncular.json` (D):** the same 9 players' `son`/`gun_30g` changed; `gunler` is unchanged.
- **Unchanged:** Rev 21 all-or-nothing (the <64 line is still only "izlenen 64", alphabetical, no label), KAPSAM-SAYI 64/64, and İLK-EKRAN (local 280/773, worst case 280/799). Under ILK_EKRAN_BOZ the shorter rail now gives 435/929.
- **smoke K2 additions:** every rail name must link to a `#g` anchor, and "Bugün N" must equal `len(set(tag_player_headlines(day's items)))`, which is 14 on 23 Sep.

Results: builds (normal, thales, normal), `check_reports` default plus all four modes, the four `test_*.py` scripts and `smoke.py` are all green.
